device_reachable = True

if device_reachable:
    print("Device is reachable.")
else:
    print("Device is not reachable.")
# This script checks if a device is reachable and prints the appropriate message.
# This script demonstrates the use of an if-else statement in Python.

devices = ["R1", "R2", "R3"]

for device in devices:
    print(f"Connecting to {device}...")
# This script iterates over a list of devices and prints a connection message for each.
# This script demonstrates the use of a for loop in Python.

count = 1
while count <= 3:
    print(f"Count is {count}")
    count += 1
# This script uses a while loop to print the count from 1 to 3
# and increments the count in each iteration.
# This script demonstrates basic control flow constructs in Python.
